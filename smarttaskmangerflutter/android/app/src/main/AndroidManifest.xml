<?xml version="1.0" encoding="utf-8"?>
<!-- AndroidManifest.xml: Declares permissions needed by the app -->
<!-- Place this at: android/app/src/main/AndroidManifest.xml -->
<!-- REPLACE the existing AndroidManifest.xml with this content -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET: Required for HTTP API calls -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- READ STORAGE: Required for image_picker and file_picker on Android < 13 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- READ MEDIA for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>

    <!-- WRITE STORAGE: May be needed for file operations -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:label="Smart Task Manager"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, for -->
    <!-- temporary picker activities. -->
    <queries>
        <!-- required for file picker  -->
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT"/>
        </intent>
    </queries>
</manifest>
